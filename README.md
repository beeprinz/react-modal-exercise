exercise with create react app and modals:

setup to start:
npm i -g create-react-app

cd into folder you want new project in then:
create-react-app modals(app name)

styled components:
npm install styled-components


